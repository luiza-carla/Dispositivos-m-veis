import {StyleSheet} from 'react-native';


const styles = StyleSheet.create({
  resultado: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#4CAF50', 
    marginTop: 20, 
    textAlign: 'center',
  
  },
});

export {styles}