import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20, 
    marginBottom: 10, 
    alignItems: 'center', 
  },
  texto: {
    fontSize: 30, 
    fontWeight: 'bold', 
    color: '#333',
    textAlign: 'center',
  },
});

export {styles}