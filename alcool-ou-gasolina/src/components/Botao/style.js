import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  botao: {
    backgroundColor: '#ee7895',
    padding: 12,
    borderRadius: 8,
    alignItems: 'center',
    marginBottom: 15
  },
  texto: {
    color: '#fff',
    fontWeight: 'bold'
  }
});
